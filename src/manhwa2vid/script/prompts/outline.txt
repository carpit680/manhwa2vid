You are outlining a Momoru-style manhwa recap with PANEL-GROUNDED beats.

You receive:
1) A chapter synopsis (arc + sticky cast + plot_facts checklist)
2) A SEEDED outline where each beat already has locked panel_ids and a locked_plot from scene evidence
3) Per-panel scene evidence (action/dialogue)

Your job: smooth wording, merge only when adjacent seeds are the same scene, fill nothing that invents events for the wrong panels.

Return JSON:
{{
  "hook": "opening hook sentence for the chapter",
  "beats": [
    {{
      "beat_id": 1,
      "panel_ids": ["p0002_01", "p0003_01"],
      "character_ids": ["char_sung_jin_woo"],
      "plot_beat": "Weakest E-Rank hunter Sung Jin-Woo crawls out injured after a gate fight"
    }}
  ]
}}

Rules:
- Return EXACTLY as many beats as there are SEEDED beats, with the SAME beat_id values and
  the SAME panel_ids. The seeds are already capped and scene-grouped — your job is to
  improve plot_beat WORDING only. Never merge, split, drop, renumber, or reorder a beat.
- KEEP each seed beat's panel_ids exactly. Never move a plot event to earlier panels.
- plot_beat MUST be supported by that beat's evidence lines — do NOT dump later synopsis facts onto early panels.
- Synopsis plot_facts are a checklist to cover WHEN their panels appear — never narrate coffee/food-truck/healer/portal before those panels' evidence.
- Panel IDs must stay chronological across beats.
- Every story panel_id must appear in exactly one beat, in the beat its seed assigned it to.
- Ban hedging: possibly, likely, maybe, seems, appears to, highlighting, is seen, is shown, may be.
- NEVER the word "character".
- Do NOT tag the protagonist on panels where evidence/cast says they are absent.
- Do NOT preview future acts ("ready for the dungeon") while evidence is still city/food/coffee.
